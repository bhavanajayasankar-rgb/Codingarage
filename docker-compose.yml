services:
  postgres:
    image: postgres:16
    container_name: codingarage-finops-postgres
    environment:
      POSTGRES_DB: codingarage_finops
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d codingarage_finops"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
